# Local FLUX.1-dev (Diffusers) — same stack as the official HF notebook / Colab.
# Install in a venv, then: pip install -r requirements-flux.txt
#
# Needs a CUDA GPU with enough VRAM for comfortable runs, or set AURABOX_FLUX_LOW_VRAM=1 (slower).
torch>=2.1.0
diffusers>=0.31.0
transformers>=4.44.0
accelerate>=0.33.0
sentencepiece
protobuf
safetensors
pillow
