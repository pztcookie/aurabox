/**
 * GitHub Pages / static hosting: set your deployed image API URL here once.
 * Example after Railway deploy: "https://your-service.up.railway.app/api/flux-generate"
 *
 * Priority: ?api= in the URL (highest) → this value → same-origin /api/flux-generate (local dev).
 * Leave empty ("") to use ?api= only, or run everything via python3 server.py locally.
 */
window.AURABOX_DEFAULT_REMOTE_API = "";
